<cover-view class='tab-bar'>
  <cover-view wx:for="{{list}}" key="{{index}}" class='tab-bar-item' data-path="{{item.pagePath}}" data-index="{{index}}" bind:tap="onChange">
    <cover-view class="   {{  item.pagePath==pagePath ?  'selectedColor':''}}        ">{{item.text}}</cover-view>
  </cover-view>
  <cover-view class='tab-bar-border'></cover-view>
</cover-view>